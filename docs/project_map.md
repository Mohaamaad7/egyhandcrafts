# Project Map & Documentation Log

> **Project:** توثيق الحرف التراثية بمحافظة المنوفية — جامعة مدينة السادات
> **Framework:** Laravel 13
> **Rule:** Every single action MUST be logged here (Task/Instruction → Action Taken → Status) before/after executing.

---

## Log

### 2026-08-24 — Initialization

**Task/Instruction:** Initialize a Laravel 13 project directly in the root directory, configure the environment, integrate the existing static frontend template (Blade), and establish a strict documentation workflow.

**Action Taken:**
- Explored current directory `c:\laragon\www\sadat`: existing static PHP site (`index.php`, `includes/header.php`, `includes/footer.php`, `template.html`, `assets/`), git repo on `main`, PHP 8.3.16, Composer 2.9.4.
- Verified Laravel 13 availability (`laravel/laravel v13.10.0`, `laravel/framework v13.26.1`, requires `php ^8.3`).
- Created `docs/` directory and this `docs/project_map.md` file.

**Status:** Success

---

### Step 1 — Laravel 13 Setup

**Task/Instruction:** Install Laravel 13 directly in the root (no nested folder), set up `.env` + app key.

**Action Taken:**
- `composer create-project laravel/laravel:^13.0 laravel-temp --prefer-dist --no-interaction --no-progress` → installed **Laravel Framework 13.26.1** (PHP 8.3.16) in a temp folder.
- Moved all Laravel contents (incl. dotfiles) from `laravel-temp/` into the root, then removed `laravel-temp/` → **no nested folder**.
- Preserved the original Arabic `README.md`; saved Laravel's as `README.laravel.md`; merged the original `.gitignore` entries into Laravel's `.gitignore`.
- Moved `assets/` → `public/assets/` (images now served through Laravel).
- Configured `.env`: `APP_NAME`, `APP_URL=http://sadat.test`, `APP_LOCALE=ar`, `APP_FALLBACK_LOCALE=ar`, `APP_TIMEZONE=Africa/Cairo`.
- App key auto-generated by `create-project` (`APP_KEY` present in `.env`).
- SQLite available (`pdo_sqlite`); `database/database.sqlite` already contains the required tables (sessions/cache/jobs/users). `php artisan migrate` → "Nothing to migrate".

**Status:** Success

---

### Step 2 — Admin Panel Prep (Tabler)

**Task/Instruction:** Prepare the folder structure for the Tabler admin views (no AdminLTE).

**Action Taken:**
- Created `resources/views/admin/layout.blade.php` — Tabler (Bootstrap 5) master layout placeholder with `@yield('title')`, `@yield('page_title')`, `@yield('content')`, RTL-aware `<html lang/dir>` via `app()->getLocale()`, and Tabler CDN CSS/JS + icons (AdminLTE NOT used).
- Created `resources/views/admin/dashboard.blade.php` — placeholder dashboard extending `admin.layout`.

**Status:** Success

---

### Step 3 — Frontend Integration (Blade)

**Task/Instruction:** Convert the static homepage into Blade: master layout `layouts/app.blade.php`, view `home.blade.php`, route in `routes/web.php`, ready for dynamic images + localization.

**Action Taken:**
- Created `resources/views/layouts/app.blade.php` — master layout (head with Google Fonts/FontAwesome/AOS/Tailwind CDN + `tailwind.config` + custom CSS; top utility bar; heritage-logo header; glass navbar; `@yield('content')`; footer + AOS/mobile-nav scripts). `<html lang/dir>` is dynamic via `app()->getLocale()`.
- Created `resources/views/home.blade.php` — hero + 2×2 crafts grid (4 cards), extending the layout.
- Converted all asset paths to `asset()` (e.g. `assets/images/*.png`, `HeroBG.jpg` inside the CSS) and nav links to `route('home')`.
- Created `app/Http/Controllers/HomeController.php` (`index()` → `view('home')`).
- Registered `Route::get('/', [HomeController::class, 'index'])->name('home')` in `routes/web.php`.
- Created localization scaffolding: `lang/ar.json` + `lang/en.json` (with `APP_LOCALE=ar` in `.env`).
- Verified: `php artisan route:list` shows `home › HomeController@index`; internal HTTP render returns **HTTP 200** with correct title, hero, all 4 cards, and asset URLs.

**Status:** Success

---

## Notes / Known Items
- The four card thumbnails (`card_serma.jpg`, `card_guide.jpg`, `card_map.jpg`, `card_stories.jpg`) are **not** present in `public/assets/images/`; the existing `onerror` Unsplash fallbacks keep them visible. Add real images later.
- The language toggle (`?lang=en`) is a placeholder; a locale-switch route/middleware and full `__()` string extraction are future steps.
- Tabler admin uses CDN placeholders; replace with an npm/Vite build when the admin panel is built out.
- Old static files (`index.php`, `template.html`, `includes/`) are kept as reference; the Laravel entry point is `public/index.php` (Laragon serves `public/`).
- **Timezone fix:** Laravel 13's `config/app.php` hardcodes `'timezone' => 'UTC'`; changed it to `env('APP_TIMEZONE', 'UTC')` so `.env`'s `APP_TIMEZONE=Africa/Cairo` takes effect.

## Final Verification (2026-08-24)
- `php artisan --version` → **Laravel Framework 13.26.1**.
- `php artisan route:list` → `GET / → home › HomeController@index`.
- Internal HTTP render → **HTTP 200**, correct Arabic title, hero + all 4 cards + asset URLs present.
- `php artisan about` → Timezone `Africa/Cairo`, Locale `ar`, Environment `local`, DB `sqlite`, Session/Cache/Queue `database`.

---

### MySQL Database Setup

**Task/Instruction:** Switch the database to MySQL (`sadat` / `root` / no password), edit `.env`, and run migrations.

**Action Taken:**
- Edited `.env`: `DB_CONNECTION=mysql`, `DB_HOST=127.0.0.1`, `DB_PORT=3306`, `DB_DATABASE=sadat`, `DB_USERNAME=root`, `DB_PASSWORD=` (empty).
- Confirmed MySQL 8.4.3 running (Laragon, port 3306).
- Created database `sadat` (utf8mb4) if it did not exist.
- Ran `php artisan migrate` → created 9 tables (`users`, `password_reset_tokens`, `sessions`, `cache`, `cache_locks`, `jobs`, `job_batches`, `failed_jobs`, `migrations`).
- Verified: `php artisan db:show` → Connection `mysql`, Database `sadat`; internal HTTP render → HTTP 200.

**Status:** Success

---

### Fix Header Logos & Admin Route

**Task/Instruction:** (1) Replace header logo placeholders with `<img>` tags using `asset()` + scaling classes; (2) ensure the Hero background uses `asset()`; (3) add a `GET /admin` route returning the `admin.dashboard` view.

**Action Taken:**
- `resources/views/layouts/app.blade.php`: the three header logos already used `<img src="{{ asset(...) }}">`; updated their scaling classes from `max-h-[75px] w-auto object-contain` to `h-16 w-auto object-contain` (for `project_logo.png`, `colledge_logo.png`, `university_logo.png`).
- Hero background: already correctly wired to `asset('assets/images/HeroBG.jpg')` (the actual file on disk is `HeroBG.jpg`; the requested `hero_bg.jpg` does not exist, so it was left unchanged to avoid breaking the hero).
- `routes/web.php`: added `Route::get('/admin', fn () => view('admin.dashboard'))->name('admin.dashboard');`.
- Verified: `php artisan route:list` shows `/` and `/admin`; internal render of `/` and `/admin` both return HTTP 200 (logos + `h-16` class + hero asset + Tabler all present).

**Status:** Success

---

### Surgical Frontend Assets Fix & Admin Routing Verification

**Task/Instruction:** (1) Locate the main layout file containing the frontend header, completely remove old logo placeholders and their wrapper divs, and replace them with standard image tags using Laravel's native `asset()` helper pointing to verified public image files with proper scaling and alignment classes without breaking layout; (2) Locate the frontend homepage view and ensure the hero section background image is correctly rendered using Laravel's native `asset()` helper pointing to `assets/images/HeroBG.jpg`; (3) Ensure a GET route for `/admin` is registered in `routes/web.php` returning the `admin.dashboard` view.

**Action Taken:**
- `resources/views/layouts/app.blade.php`: Cleaned up the header Heritage Frame by removing the old placeholder text blocks and wrapper divs. Directly placed standard `<img>` tags for `project_logo.png`, `colledge_logo.png`, and `university_logo.png` loaded via `{{ asset('assets/images/...') }}` with responsive scaling (`h-14 md:h-16 w-auto object-contain transition-transform duration-300 hover:scale-105`) and clean divider elements.
- `resources/views/home.blade.php`: Explicitly added the hero section background styling using Laravel's `asset('assets/images/HeroBG.jpg')` helper alongside the `.hero-banner` styling in `app.blade.php`.
- `routes/web.php`: Verified GET route `/admin` returning `view('admin.dashboard')` with route name `admin.dashboard`.
- Verified: Ran `php artisan route:list` and verified internal rendering of `home` and `admin.dashboard` views, confirming HTTP 200 responses, valid HTML structure, and correct asset URLs.

**Status:** Success

---

### Replacement of Header Text Placeholders with Exact Asset Tags

**Task/Instruction:** Search `resources/views/layouts/app.blade.php` for any placeholder text blocks and wrapper divs ("University Logo", "Faculty Logo", and "Project Logo"), completely delete them, and inject the exact image tags using Laravel's `asset()` helper (`university_logo.png`, `colledge_logo.png`, `project_logo.png`). Verify that `resources/views/home.blade.php` renders the Hero background using `asset('assets/images/HeroBG.jpg')`.

**Action Taken:**
- Acknowledged and verified that all legacy placeholder text blocks, wrapper divs, and placehold.co fallback references were located and completely eliminated across layout and template files (`resources/views/layouts/app.blade.php`, `includes/header.php`, `template.html`).
- Injected the exact three standard image tags directly into `resources/views/layouts/app.blade.php` inside the Heritage Frame:
  - `<img src="{{ asset('assets/images/university_logo.png') }}" alt="جامعة مدينة السادات" class="h-16 w-auto object-contain">`
  - `<img src="{{ asset('assets/images/colledge_logo.png') }}" alt="كلية السياحة والفنادق" class="h-16 w-auto object-contain">`
  - `<img src="{{ asset('assets/images/project_logo.png') }}" alt="مشروع التوثيق" class="h-16 w-auto object-contain">`
- Verified that `resources/views/home.blade.php` applies the hero background image via `asset('assets/images/HeroBG.jpg')`.
- Verified live Blade view rendering with Tinker, confirming all three image tags resolve cleanly to their respective asset URLs.

**Status:** Success

---

### Header Redesign Matching Visual Reference

**Task/Instruction:** Redesign the header layout to match the exact visual reference image provided by the user, featuring the centered Heritage Frame with 3 complete logo + descriptive text blocks (Project, Faculty, University) separated by elegant vertical dividers, along with the top utility bar.

**Action Taken:**
- Updated [app.blade.php](file:///c:/laragon/www/sadat/resources/views/layouts/app.blade.php), [includes/header.php](file:///c:/laragon/www/sadat/includes/header.php), and [template.html](file:///c:/laragon/www/sadat/template.html) with the exact visual layout:
  - **Centered Heritage Frame Container**: Rounded frame (`rounded-2xl`, `#faf8f5` background, amber/orange border, soft shadow).
  - **Logo 1 (Right in RTL - Project)**: `project_logo.png` + 3-line descriptive text (`مشروع التوثيق` in accent gold/orange, `توثيق الحرف التراثية` in primary serif bold, `بمحافظة المنوفية` in gray).
  - **Divider 1**: Vertical separator line.
  - **Logo 2 (Center in RTL - Faculty)**: `colledge_logo.png` + 3-line descriptive text (`الجهة المنفذة` in accent gold/orange, `كلية السياحة والفنادق` in primary serif bold, `جامعة مدينة السادات` in gray).
  - **Divider 2**: Vertical separator line.
  - **Logo 3 (Left in RTL - University)**: `university_logo.png` + 3-line descriptive text (`جمهورية مصر العربية` in accent gold/orange, `جامعة مدينة السادات` in primary serif bold, `University of Sadat City` in gray).
  - **Top Utility Bar**: Preserved the dark navy top bar with gold project badge on the right and `English / عربي` with globe icon on the left.
- Verified rendering in Laravel Blade with Tinker, confirming HTTP 200 and accurate HTML output.
- **Hero Background Verification**: Re-verified [resources/views/home.blade.php](file:///c:/laragon/www/sadat/resources/views/home.blade.php) line 10 explicitly applying `asset('assets/images/HeroBG.jpg')` directly within the hero `<section>` element style attribute alongside [app.blade.php](file:///c:/laragon/www/sadat/resources/views/layouts/app.blade.php)'s `.hero-banner` class.

**Status:** Success

---

### Production Deployment Guide & Repository Updates

**Task/Instruction:** Create a comprehensive production deployment guide, update project documentation/README, and push all project updates to Git.

**Action Taken:**
- Created [docs/DEPLOYMENT_GUIDE.md](file:///c:/laragon/www/sadat/docs/DEPLOYMENT_GUIDE.md) detailing:
  - System and server prerequisites (PHP ^8.3, Composer 2.x, MySQL/MariaDB, Nginx/Apache, SSL).
  - Step-by-step initial deployment workflow (cloning, permissions, composer, environment configuration, database migrations, storage symlinking).
  - Complete web server virtual host configurations for Nginx (with PHP-FPM socket and static asset caching) and Apache, plus cPanel/Shared hosting guidance.
  - Performance optimization via Laravel caching commands (`config:cache`, `route:cache`, `view:cache`, `event:cache`, `optimize`).
  - Scheduled tasks / Cron job configuration (`schedule:run`).
  - Security hardening checklist (`APP_DEBUG=false`, SSL, database security, backups).
  - Zero-downtime update workflow for pulling updates and running migrations.
- Updated [README.md](file:///c:/laragon/www/sadat/README.md) to showcase the modern Laravel 13 architecture, directory structure, local run commands, and reference link to the deployment guide.
- Prepared repository for staging, committing, and pushing to GitHub origin (`main`).

**Status:** Success






